* Dmtopup header section same design * (Emoji text). make the ui like @ref.png keep the color in a wat so that we can change easily later, we need categorized products, a product can belong to multiple categories. 
* Clicking on product opens a new tab
* UI improvement add proper animation and transitions , Logo dynamic from admin panel
* Coin system (Dmtopup reference). users can claim coin and spend coin to convert to money in wallet, each purchase has a coin value.

admin @topupman-admin/src/views/
client @topupman-client/pages/
backend @topupman-api/src/routes/